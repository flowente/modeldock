name: Build Open WebUI runtime packs

on:
  workflow_dispatch:
    inputs:
      runtime_version:
        description: Runtime pack version
        required: true
        default: "6"
  push:
    tags:
      - "openwebui-runtime-v*"

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: windows-x64
            runner: windows-2025
          - target: macos-arm64
            runner: macos-15
          - target: macos-x64
            runner: macos-15-intel
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4
      - name: Resolve runtime version
        id: runtime-version
        env:
          INPUT_RUNTIME_VERSION: ${{ inputs.runtime_version }}
        run: node scripts/resolve-openwebui-runtime-version.mjs
      - uses: astral-sh/setup-uv@v7
        with:
          version: "0.8.22"
      - name: Build verified runtime pack
        run: node scripts/build-openwebui-runtime.mjs "${{ matrix.target }}" "${{ steps.runtime-version.outputs.version }}"
      - uses: actions/upload-artifact@v4
        with:
          name: modeldock-openwebui-${{ matrix.target }}
          path: .runtime-build/${{ matrix.target }}/modeldock-openwebui-${{ matrix.target }}.tar.gz
          compression-level: 0
          retention-days: 7

  publish:
    name: Publish runtime release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Resolve runtime version
        id: runtime-version
        env:
          INPUT_RUNTIME_VERSION: ${{ inputs.runtime_version }}
        run: node scripts/resolve-openwebui-runtime-version.mjs
      - uses: actions/download-artifact@v4
        with:
          path: runtime-assets
          merge-multiple: true
      - name: Create verified manifest
        env:
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          node scripts/create-openwebui-runtime-manifest.mjs "${{ steps.runtime-version.outputs.version }}" "openwebui-runtime-v${{ steps.runtime-version.outputs.version }}" runtime-assets
      - name: Publish release assets
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: openwebui-runtime-v${{ steps.runtime-version.outputs.version }}
        run: |
          gh release view "$TAG" >/dev/null 2>&1 || gh release create "$TAG" --title "Open WebUI runtime v${{ steps.runtime-version.outputs.version }}" --notes "Verified Open WebUI runtime packs used by ModelDock."
          gh release upload "$TAG" runtime-assets/*.tar.gz runtime-assets/manifest.json --clobber
